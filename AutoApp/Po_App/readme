                PageObject模式原则
1、方法意义
    a、用公共方法代表UI所提供的功能
    b、所有的方法应该返回其他的PageObject
    c、同样的行为不同的结果可以建模为不同的方法

2、字段意义
    a、不要暴露页面的元素给外部
    b、不需要建模 ui内所有元素
    c、不要在PO模式中加入断言，应该应用在测试用例中

3、设计原理
    a、提取selenium中公用的方法，作为基本对象
    b、页面业务流程的关联设计
    c、调用业务流程实现自动化测试

4、优势
    a、将不同的页面对象封装成不同的页面对象
    b、进一步降低冗余
    c、对于代码的阅读性和维护性有进一步提升
